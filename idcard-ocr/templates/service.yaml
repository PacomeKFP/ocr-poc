apiVersion: v1
kind: Service
metadata:
  name: { { include "idcard-orc.fullname" . } }
  labels: { { - include "idcard-orc.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "idcard-orc.selectorLabels" . | nindent 4 } }
